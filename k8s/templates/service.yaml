apiVersion: v1
kind: Service
metadata:
  name: kill-me
  namespace: kill-me
  labels:
    app: kill-me
spec:
  type: ClusterIP
  selector:
    app: kill-me
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
