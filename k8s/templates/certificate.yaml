apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kill-me-tls
  namespace: kill-me
spec:
  secretName: kill-me-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - workout.romaine.life
